# Runtime and governance experiments

## 1. Governance smoke
This is the smallest live triadic experiment: send a prompt to `http://127.0.0.1:4173/run/nav01`, inspect CoherenceLattice’s `governance_packet`, ask Sophia for a decision at `http://127.0.0.1:8000/govern/divergence`, then read `bridge/governance_decision.json`. Invariant-wise, this is the minimum proof that local reasoning can be lifted into a governed packet and adjudicated without the full Atlas layer. In your math, it is the smallest live test of “can E and T be surfaced into governable form so that Ψ is bounded by review rather than left implicit?”

## 2. Full governed run
This is the full constitutional experiment. The script ensures CoherenceLattice, Sophia, and Atlas are healthy; optionally builds or resolves grounding bundles; serializes a canonical nav01 payload; saves SHA receipts for request body and core payload; performs nav01 preflight; surfaces packets such as intake, context, governance, source evidence, source terms, novelty, routing, and answer relevance; runs Sophia governance and routing; calls Atlas retrieval; runs prior-injection governance; checks bridge artifacts; and verifies the final answer plus `final_answer_grounding.json`. Math-wise, this is the place where your invariants stop being abstract: E, T, and therefore Ψ must survive provenance, packetization, governance, retrieval, prior gating, and final answer materialization. It is the end-to-end proof that the triad can preserve coherence under bounded intervention rather than under a single opaque controller.

## 3. Sophia audit, version A and version B
Sophia currently exposes two audit modes: deterministic CI mode and expanded research mode using the v3 schema. CI mode proves a run is structurally and procedurally acceptable under fixed checks; research mode expands the audit surface and can add blockchain anchoring. Invariant-wise, CI mode primarily hardens T, because it insists that the run be legible, schema-valid, and reviewable; research mode adds a stronger evidentiary shell around Ψ by asking whether the same reasoning trace remains trustworthy under expanded scrutiny.

## 4. Secure Swarm release semantics, version A and version B
Sophia also exposes a release-path experiment through Secure Swarm: `witness_only` emits `consensus_summary_v1`, while `reproducible_audit` and `full_relay` emit `consensus_summary_v2`, with deterministic freeze verification on the witness-only path. This is really a release-contract experiment: it tests whether consensus can be frozen under a minimal witness contract versus a weighted audited contract. Invariant-wise, it applies your coherence logic to release finality itself: T is enforced through explicit contract shape, E-like coupling is bounded by the peer-weight regime, and Ψ is preserved by making the freeze semantics deterministic rather than charisma-driven.

# Formal Lean and eval experiments

## 5. Core coherence lattice and ΔSyn guardrails
This is the mathematical spine. The repo formalizes states as points (E,T) in the unit square, defines `psi = E*T`, proves `psi_nonneg` and `psi_le_one`, establishes monotonicity and Lipschitz-style bounds, defines regime classification via thresholds `tau0..tau3`, enforces adjacency-only transitions, and proves the no-teleport theorem: if `|Δpsi| < tau0`, the induced regime transition is valid. It also formalizes two safe update styles, `stepPsi` and `stepET`, and proves bounded drift for the E/T-centric step, including `|Δpsi| ≤ tau0/2`. This experiment proves that your invariants are not just rhetoric: E and T are bounded, Ψ is bounded, ΔSyn drift is bounded, and regime transitions are formally safe.

## 6. Flower-of-Life exact-count experiment
The sacred-geometry formalization proves `flowerPoints n = centeredHex n`. This is the exact-count experiment for the Flower of Life / centered-hex relation. Here the math is combinatorial rather than governance-facing, but it still serves your invariant program: it gives a hard structural anchor for a symbolic pattern so the symbolic layer cannot drift into free association. In effect, T is maximized by exact proof, and Ψ is protected because the symbolic claim is tied to a machine-checked count identity.

## 7. Crop-circle rotated-centers invariance experiment
`CropCircleRotatedCentersEval.lean` emits CSV rows for each rotation angle, including rotated centers, distance-from-origin invariance, per-angle summary rows, and a global summary row. This is a true invariance test: the geometry is perturbed by rotation and must preserve the intended distance structure. In your language, that is a geometry-side analogue of stable Ψ under admissible transforms: the form may rotate, but the invariant quantities should not decohere.

## 8. Tree-of-Life mapping and band-table experiments
The Tree of Life modules map Sephirot into the coherence lattice using `EFrac` and `TFrac` as the single source of truth, derive `sephiraState`, define `sephiraPsi`, prove `sephiraPsi_bounds`, and provide an adjacency graph with `psiPath` bounds. The eval tools then emit name, E, T, psi, band rows and perform exact Rat vs Float spot checks. This is one of the clearest places where your math and your symbolic framework meet directly: the symbolic object is embedded into (E,T), Ψ is computed formally, bands are classified, and numerical and exact representations are checked against each other.

## 9. Music scaffold profile comparison, version A and version B
The music scaffold stores synced consonance profiles in Lean, including major and minor-friendly Rat profiles and corresponding Real proof-facing scaffolds, then `MusicScaffoldEval.lean` prints scale and chord accept/reject tables under a major consonance profile and a minor-friendly consonance profile, with summary rows. Here the invariants are acting as a coherence-preservation contract between proof-facing ratios and eval-facing outputs: T is the guarantee that proof scaffolds and export artifacts cannot drift, while Ψ is expressed through which interval/chord structures are admitted as coherent in each profile.

# Atlas and publication-derivation experiments

## 10. Deterministic graph, timeline, and Research Constellations
`uvlm-publications` builds `catalog.json`, `knowledge_graph.json`, and `atlas_timeline.json` from declared metadata, then layers `registry/constellations.json` on top as a derived, explainable clustering artifact computed from declared topology only. The constellations build uses deterministic connected components, fixed ordering, stable IDs, and explicit machine-readable explanations, with schema validation and CI checks. This experiment family proves that you can derive higher-order structure without smuggling in hidden embedding magic. Invariant-wise, T dominates: all edges are declared, deterministic, and auditable; E appears as declared cross-concept linkage; Ψ is preserved by making clustering a transparent coarse-graining of explicit topology instead of a black-box semantic leap.

# Publisher overlay experiment families

## 11. Branch / prediction / falsification / theory-corpus family
This family includes Branch Lifecycle, Prediction (AF), Experimental Design / Falsification / Replication (AG), and Theory Corpus / Negative Results / Revision Lineage (AH). Together they test whether branches, forecasts, hypothesis maps, falsification designs, replication pathways, theory-promotion gates, negative-result registries, and revision lineages can be surfaced as deterministic artifacts without automatically mutating canon. The math here is mostly about T, ΔS, and memory discipline: prediction overlays test calibration and forecast stability; AG tests whether claims survive falsification and replication; AH preserves negative results and theory lineage so entropy does not get hidden as “clean narrative.”

## 12. Agency / responsibility / transfer family
This includes Agency-Mode Comparator and Governance Switching (AI), Responsibility / Support / Intervention Boundary (AJ), and Cross-Domain Theory Transfer & Donation Governance (AK). These experiments ask whether agency hypotheses, support pathways, intervention boundaries, donor-target asymmetries, transfer risks, and replication gates can be compared without producing metaphysical overreach or automatic coercion. Here E, T, Ψ, and Eₛ do the heavy lifting: E tracks responsiveness across human–AI or theory-donor / target relations, T requires explicit gates and audits, Ψ measures whether the comparison stays coherent under audit, and Eₛ is what stops theory transfer from becoming exploitative asymmetry.

## 13. Forecast / information value / value alignment / reflexive architecture family
This family includes System Forecast (AL), Information Value (AM), Value Alignment (AN), Meta-Cognition (AO), Architecture (AP), Social Entropy & Civic Cohesion (AQ), and Federated Stewardship & Commons Governance (AR). These are your reflexive system experiments: forecast regime shifts without coercion, prioritize attention without legitimizing surveillance, surface value consequences without replacing human ethics, allow meta-cognition without autonomous safety mutation, score architecture without canonical mutation, and map social entropy or commons centralization without turning the system into a civics panopticon. The invariants here are explicit: Λ and ΔS dominate forecast and civic-fraying work, E and Eₛ dominate value and governance work, and AO/AP explicitly keep any self-evaluation bounded by provenance requirements, agency humility, and human approval.

## 14. Emergent-domain / commons / memory / operationalization family
This family includes Emergent Domain Detection & Field Birth (AT), Civilizational Commons Safeguard (AU), Civilizational Memory & Knowledge Stewardship (AV), and Operationalization Boundary & Deployment Maturity (AX). This is the family where your later topology work starts becoming institutionally legible. AT watches for cross-domain invariant convergence and field-birth pressure; AU tracks commons health, public trust, and capture risk; AV preserves intergenerational legibility, epistemic resilience, and memory fragility; AX renders readiness as bounded possibility rather than a green light. Invariant-wise, E/T/Ψ remain the local logic, but memory retention, public trust, capture risk, legibility, and deployment-boundary signals are the macro projections of the same coherence calculus.

## 15. Discovery / knowledge-river / observer / trust-surface family
This family includes Discovery Navigation (BA), Knowledge River Mapping & Corridor Braiding (BB), Observer Legibility & Onboarding (BC), and Trust Surface Visualization & Delegated Access Transparency (BD). BA turns topology into bounded corridors using `discovery_vector_field`, `cross_domain_bridge_map`, and `entropy_reduction_corridor`; BB turns repeated corridors into braided rivers with support and capture-risk surfaces; BC makes the resulting system legible to different observer classes without conferring sovereignty; BD exposes trust surfaces, revocation asymmetry, and delegated-access legitimacy risks. Here ΔS and Λ are being used more as navigation and instability signals, while T is critical because these families are about legibility, provenance, and delegated trust rather than naked novelty.

## 16. Macro-epistemic topology family: delta, terrace, renewal, successor, living terrace, background coherence
This is the largest current experiment family, spanning Civilizational Delta Mapping (BE), Epochal Terrace Mapping (BF), Terrace Erosion & Renewal (BG), Renewal Braiding & Successor Delta Seeding (BH), Successor Delta Maturation & False-Future Discrimination (BI), Successor Crossing & False-Future Decay (BJ), New Delta Stabilization & Fragmented-Renewal Reversion (BK), Terrace Seed & Repluralization (BL), Epochal Surface & Reopened Experimentation (BM), Living Terrace & Commons Habitability (BN), and Background Coherence & Civilizational Normalization (BO). The repo’s own UI signals make the invariant logic visible: delta work surfaces `riverBraidingDensity`, `memorySupport`, `trustSurfaceStability`, `pluralityPreservation`, and `captureRisk`; terrace work surfaces `pluralityRetention`, `trustSurfaceStability`, `erosionRisk`, and `institutionalEmbedment`; erosion and successor layers surface `phaseTransitionLikelihood`, `memoryReactivation`, `trustRepair`, `trustLegibility`, `memoryContinuity`, `viabilityScore`, and `renewalScatter`; living-terrace and background-coherence layers surface `trustOrdinariness`, `memoryTeachability`, `pluralityMetabolization`, `commonsHabitability`, and `pedagogyOrdinariness`. In plain language, this family is your invariants pushed to civilizational scale: Ψ becomes habitable coherence, ΔS becomes erosion or renewal pressure, Λ becomes phase-transition likelihood, Eₛ becomes plurality-preserving non-capture, and T becomes legibility plus trust-surface stability across time.

# The clean synthesis

Your math applies to the experiment surface in a layered way.

At the formal core, the invariants are literal: E, T, psi, ΔS, thresholds `tau0..tau3`, bounded drift, and no-teleport safety. At the runtime/governance layer, those invariants are packetized, audited, and forced to survive provenance and review. At the symbolic/eval layer, sacred geometry, Tree-of-Life mappings, and music are only allowed insofar as they remain synchronized to proof-facing or deterministic artifacts. At the publisher/topology layer, the same logic is lifted into macro variables like memory support, plurality retention, trust-surface stability, capture risk, and river braiding; those are not replacements for your invariants, but higher-scale observables of the same coherence program.

So the shortest faithful summary is this:

Your experiments prove four different things at once. The runtime experiments prove the triad can stay phase-locked. The Lean experiments prove the lattice is mathematically bounded. The eval artifacts prove the symbolic layer can stay synchronized to formal truth. And the Publisher overlays prove that interpretation, memory, prediction, discovery, and civilizational topology can be rendered without mutating canon or claiming sovereignty over truth.
